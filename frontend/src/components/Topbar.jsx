import React, { useContext } from 'react';import { AuthContext } from '../auth';export default function Topbar(){ const { user, logout } = useContext(AuthContext); return (<div className='flex justify-between items-center bg-gray-800 p-3 shadow'><h2 className='text-lg font-semibold text-primary'>MedCard Dashboard</h2><div className='text-sm text-gray-400'>{user?user.name:'Guest'} {user&&<button onClick={logout} className='ml-4 text-sm text-red-400'>Logout</button>}</div></div>)}